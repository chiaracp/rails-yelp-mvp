<h1>Restaurants</h1>
<ul>
<% @restaurants.each do |restaurant| %>
  <%= restaurant.name %>
  <li><%= link_to "see more", restaurant_path(restaurant), class: "btn btn-primary"%></li>
<% end %>
</ul>

  <%= link_to "Add new restaurant", new_restaurant_path, class: "btn btn-warning p-2"%>
